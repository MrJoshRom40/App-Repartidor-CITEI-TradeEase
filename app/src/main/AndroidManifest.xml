<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Repartidor"
        tools:targetApi="31">

        <activity
            android:name=".Carga_Descarga"
            android:exported="false" />
        <activity
            android:name=".formulario2"
            android:exported="false" />
        <activity
            android:name=".formulario1P2"
            android:exported="false" />
        <activity
            android:name=".Formulario1P1"
            android:exported="false" />
        <activity
            android:name=".Descanso"
            android:exported="false" />
        <activity
            android:name=".Reporte"
            android:exported="false" />
        <activity
            android:name=".Problema"
            android:exported="false" />
        <activity
            android:name=".Inicio"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".NotificationWorker"
            android:enabled="true"
            android:exported="true" />
        <receiver android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Si tienes otros receivers para Continuar y Regresar, no es necesario que sean BroadcastReceiver -->
        <!-- Asegúrate de que estas clases estén implementadas como Activities o BroadcastReceivers según sea necesario -->
        <receiver android:name=".Continuar" android:exported="true" />
        <receiver android:name=".Regresar" android:exported="true" />

    </application>

</manifest>
